[tools]
node = "24.18.0"
# pnpm via the npm backend: the default aqua backend has an asset-name
# mismatch for this version (expects pnpm-macos-arm64; pnpm ships
# pnpm-darwin-arm64.tar.gz). npm backend is pure-JS and cross-platform.
"npm:pnpm" = "11.9.0"
